specVersion: "v1beta"
serviceId: "bigbike-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bigbike-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
